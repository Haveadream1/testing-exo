function sum(a, b) {
    return a + b;
}

module.exports = sum;

// 1. Create the file we want to Test
    // Use : module.exports = fct_name;

// 2. npm init -y
    // Answer the question

// 3. Install jest as a dependencies
    // npm i jest -D

// 4. Create the test file
    // Add tests

// 5. Go to the package.json
    // Change: "test": "jest"